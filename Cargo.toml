[workspace]
members = [
    "samples/counter",
    "samples/naming_system",
    "samples/fib",
    "ic-kit",
    "ic-kit-sys",
    "ic-kit-macros",
    "ic-kit-runtime",
]

[profile.release]
lto = true
opt-level = 'z'

[patch.crates-io]
num-bigint = { git = "https://github.com/rust-num/num-bigint" }
